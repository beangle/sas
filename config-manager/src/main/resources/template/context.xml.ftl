<Context path="${context.path}" reloadable="false">
<Resource 
  name="${resource.name}"
  driverClassName="${resource.driverClassName}"
  url="${resource.url}"
  type="javax.sql.DataSource"
  username="${resource.url}"
  password="${resource.password}"
  />
</Context>
